@using ISM_Final_Lahiye.Areas.Adminpanel.Models;

@model IEnumerable<Users>


@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Adminpanel/Views/Shared/_Layout.cshtml";
}
<a href="@Url.Action("Insert", "User")" class="btn btn-success"><i class="icon icon-user-follow"></i> Add new user</a>
<br />
<br />


<div class="card mb-3">
    <div class="card-header">
        <i class="fa fa-table"></i> All Users
    </div>
    <div class="card-body" id="user">
        <div class="table-responsive">
            <table class="table table-bordered" id="dataTable" cellspacing="0">
                <thead>
                    <tr>
                        <th>Username</th>
                        <th>Email</th>
                        <th>Photo</th>
                        <th></th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr class="text-center">
                            <td>@item.user_username</td>
                            <td>@item.user_email</td>
                            <td>
                                @if (item.user_img == "")
                                {
                                    <img src="~/Uploads/default.png" width="50" class="rounded mx-auto d-block"/>
                                }
                                else
                                {
                                    <img src="~/Uploads/@item.user_img" width="50" class="rounded mx-auto d-block"/>
                                }
                            </td>
                            <td>
                                <a href="#" class="btn btn-danger btn-sm float-right activeAccount" data-toggle="modal" data-target="#delete_modal" data-id="@item.user_id">
                                    <i class="icon icon-trash"></i>   Delete
                                </a>
                            </td>
                            <td>
                                <a href="@Url.Action("Update", "User", new { id = item.user_id })" class="btn btn-info btn-sm float-right ">
                                    <i class="icon icon-pencil"></i>   Update
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@* Delete modal*@
<div class="modal fade" id="delete_modal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header bg-danger border-0">
                <h5 class="modal-title text-white"> Do you want delete this item ? </h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body p-5">

            </div>
            <div class="modal-footer border-0">
                <button id="cancel_user" type="button" class="btn btn-outline-primary" data-dismiss="modal">Cancel</button>
                <a href="#" id="delete_user" class="btn btn-danger">
                    Delete
                </a>
            </div>
        </div>
    </div>
</div>
